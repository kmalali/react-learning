// arrow function
let sum = ( x, y ) => {
    let result = x + y;
    return result;
};

// arrow function - only one statement and that's a return statement
let sum = ( x, y ) => x + y;

// function with single argument - argument need not be enclosed in parentheses 
let square = n => n * n;